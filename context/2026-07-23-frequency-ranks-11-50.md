# Frequency ranks 11–50 drafted — 2026-07-23

Ranks 11 (`nicht`) through 50 (`vor`) were independently authored as forty
complete text-only draft cards. Each card has one main example, three additional
examples, natural English translations, morphology or syntax guidance where
relevant, and a concise usage note. Ambiguous function words such as `zu`,
`als`, `um`, and `da` use their extra examples to cover several core uses.

The canonical output is `data/canonical/frequency-0011-0050.jsonl`, generated by
`tools/build_batch_002.py`. Frequency rank remains canonical curriculum
metadata. It is available to the review interface for navigation but has been
removed from the learner-facing back of the card.

The local viewer now loads both frequency batches for a continuous 50-card
review. After rebuilding the batch, the global validator reported 51 records
(the 50 frequency cards plus the earlier `wer` prototype) and 208 unique German
sentences.

Next: human-review ranks 1–50, revise any weak examples, and lock the approved
text before testing audio on a small representative sample.
